# gdn-deferred-checkpoints review (e0dad291 vs a8333658), read-only, no GPU

vllm/v1/worker/gpu/boundary_checkpoint.py:651: HIGH: a 4th reader of a speculative column that §6 never enumerates. `capture_mamba` -> `checkpoint_request_boundaries` (mamba_utils.py:1393) -> `checkpoint_mamba_states_kernel` (:378-437, copy at :419) reads `state_idx + capture_bias`; with the knob on it exports a record block as a state, silently poisoning boundary checkpoints and the prefix-cache export. Fix: route it through `commit_deferred_checkpoints`, or refuse `gdn_deferred_checkpoints` when boundary checkpointing is on.
vllm/v1/worker/mamba_utils.py:197-374: HIGH: §6(b)'s "the conv half keeps its token_bias, the temporal half becomes a commit" is not implementable. One `_copy_mamba_state_block` call does conv *and* temporal from a single shared bias (its own docstring, :222-231); there is no per-half call at any of :419, :557, :688. Fix: split the helper (or add a separate `temporal_bias`) before calling §6 a four-edit patch.
b12x/sequence/gdn_decode/_cute_kernels.py:192: HIGH: the bit-identity claim rests on an FMA contraction the compiler may pick the other way. In the verify loop `state_value` has two uses (:616-618) so it is materialized and the result is `fma(delta,k,rn(s*decay))`; in the replay it has exactly one use, and LLVM folds the *left* fmul first, giving `fma(s,decay,rn(delta*k))`. The pytest is the only guard and it is compiler-version-fragile. Fix: pin it — store the decayed value back (`state[e] = state[e]*decay` as its own statement) or emit an explicit fma.
b12x/sequence/gdn_decode/_cute_kernels.py:1652: MED: `precompile_commit_deferred_checkpoints` is exported by neither api.py nor __init__.py, and it only compiles — it never adds to `_COMMIT_WARMED`, so the capture guard at :1677 still raises. A graph-captured commit is unreachable through the public API. Fix: export it and have it do one warm launch.
b12x/sequence/gdn_decode/_cute_kernels.py:1371: MED: a `destination_indices` entry pointing at a speculative slot of the *same* request corrupts silently — CTAs write the destination while sibling CTAs still replay records out of it, with no grid sync. Only "negative skips / equals column 0 commits in place" is documented. Fix: reject destinations present in `state_indices[r,1:]`, or state the precondition in `_impl.py:872`.
b12x/sequence/gdn_decode/_cute_kernels.py:648: MED: with the knob on, a duplicate cell (`state_indices[r,j] == state_indices[r,0]`) stops being a redundant snapshot write and becomes a record overwriting the base state. `run()`'s uniqueness precondition is now load-bearing for correctness, not freshness. Fix: say so in __init__.py and land the `preprocess_mamba` assertion §6(d) already promises.
tests/sequence/test_gdn_deferred_checkpoints.py:135: MED: every GPU test uses one uniform accepted and `query_lengths=(5,)*n`. A per-request indexing bug in the replay length (wrong row of `num_accepted_tokens`, or a length borrowed from another request) passes. Fix: one case with mixed accepted (1,5,3,2) and mixed query lengths.
b12x/sequence/gdn_decode/_cute_kernels.py:51: LOW: the assert is a tautology over compile-time constants (704 <= 16384) and can never fire; the real bound, `recurrent_state.stride(0) >= value_heads*_VALUE_TILES*_RECORD_FLOATS`, is unchecked. Fix: check the stride beside the existing contiguity check in `_binding_key`.
docs/gdn-deferred-checkpoints.md:§6: LOW: nearly every citation is wrong against e624ae19a3. The helper is worker/mamba_utils.py:197-374 and does not exist in model_executor/layers/mamba/mamba_utils.py at all; `src_off` at :612 belongs to `preprocess_mamba_align_fused_kernel`, not `precopy_…` (:626-709); `preprocess_mamba` (:1663-1755) never calls the helper (it goes via `collect_mamba_copy_meta`/`batch_memcpy_kernel`); accepted resets are :556/:623/:1753, not :622-623/:1743; the early return is :684; `recurrent_state`/`state_indices`/`num_accepted_tokens` are bound at gdn.py:1247-1249, not :2615-2625; `additional_config` at :272; `page_size_bytes` at :955-962. Fix: re-derive §6 before anyone applies it.
b12x/sequence/gdn_decode/_cute_kernels.py:455: LOW: §4's "with the knob off the traced program is the old one statement for statement" is now inexact — both request helpers gained an unused `accepted_column` parameter and `source_column` is an extra SSA name. Semantics are unchanged; reword the claim.

Confirmed sound: accepted lengths reach both kernels purely as a device read (`num_accepted_tokens[request]`, :1038 and :1376), so nothing depends on the host and CUDA-graph replay is safe; every new branch is genuinely compile-time (`relative_token` is a `range_constexpr` Python int, `has_null_state_index` is mutually exclusive with the knob); record addressing fits, is 16-byte aligned, and never overlaps the committed state or another work item's block, so the two `sync_threads` (:506, :883) are the only ordering needed and they cover the WAR correctly; `_relocate_speculative_block` (single_type_kv_cache_manager.py:2280-2288, from :2226) only re-slots within one request's own table, so records do survive step N to N+1; the `_KEY_FIELDS` exclusion really does leave `encode_query` byte-identical; and the tests do cover both dispatches (bs1 ungrouped, bs8/16 grouped + ungrouped tail), the one-column plan, the fused-replay WAR, and — via the accepted 1..5 sweep against distinct shipped checkpoints — a wrong replay length.

SAFE TO GPU-TEST: yes
SAFE TO SERVE (with vLLM patch): after fixes
